meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{host}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"admin@example.com",
    "password":"Admin123!"
  }
}

tests {
  const responseData = res.body;
  if(responseData.data)
  {
    const token = responseData.data.token;
    if (token) {
      bru.setEnvVar("token", "Bearer "+token);
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
